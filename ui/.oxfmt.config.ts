import { defineConfig } from 'oxfmt';

export default defineConfig({
  printWidth: 100,
  semi: true,
  singleQuote: true,
  svelte: true,
  tabWidth: 2,
  trailingComma: 'all',
  useTabs: false,
});
